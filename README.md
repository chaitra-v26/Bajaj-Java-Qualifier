# Bajaj-Java-Qualifier
Spring Boot application for the Bajaj Finserv Java Qualifier that automates the entire submission workflow. It generates a webhook, retrieves a JWT token, and posts the final SQL query using RestTemplate with Spring Boot configuration. The project includes complete source code and a runnable JAR.
